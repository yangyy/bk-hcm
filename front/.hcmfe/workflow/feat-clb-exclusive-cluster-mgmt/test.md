# feat-clb-exclusive-cluster-mgmt 测试清单

> 工作流: `feat-clb-exclusive-cluster-mgmt`
> 关联文档: [prd.md](./prd.md) / [design.md](./design.md) / [api.md](./api.md) / [coding.md](./coding.md)
> TAPD: 独占集群管理页面 #1069995598138113967

## 验证范围

- 功能边界: 资源接入-负载均衡双入口（负载均衡 / 独占集群）、独占集群列表与筛选、单条/批量分配到业务，对应 AC-001~AC-005。
- 不在范围: 后端同步与表结构、购买独占型规格、单据展示、CLB 列表实例规格字段（兄弟迭代 `feat-clb-list-spec-field`）、CLB 详情独占集群字段、容量管理菜单。

## 测试环境

- 前端入口: `<测试环境「资源接入 → 负载均衡」列表页，地址由执行人在测试时确认>`
- 测试账号: `<test_account>`
- 数据准备:
  - **接口未上环境**：`load_balancers/exclusive_clusters/{list,assign/bizs}` 在 PR #2078 只有契约文档，后端接口层还在兄弟单。仓库里**没有 mock**（提交前已删除），真实环境下列表就是 404 + 空表格。
  - 因此要验列表相关用例，必须用 devtools 的响应改写（Network → Override content）给 `exclusive_clusters/list` 造返回：`data.details` 至少 3 行（1 行 TGW 有 `max_conn`、1 行 STGW `max_conn: null`、1 行 `bk_biz_id` 为真实业务 ID 代表已分配），其余行 `bk_biz_id: -1`；`count` 请求（`page.count: true`）单独返回 `data.count`。
  - 造数据时每行的 `account_id` 要填**左侧树选中的那个账号**，否则分配弹窗的目标业务下拉本就该为空（下拉是按行的 `account_id` 取业务）。
  - 不改写响应时能验的只有 P0-01 / P0-02 / P0-03 / P0-04（入口、URL、请求次数与入参）和 P1-01；其余列表/分配用例标 Skipped 并注明「等后端上环境」。
  - 分配用的目标业务：该账号有管理权限的业务至少 1 个。
  - 本项目前端无单测底座，`covered-by` 一律留空，全部为人工点。

## 用例清单

### P0 - 主流程（必测）

| ID | 场景 | 前置 | 操作 | 期望 | covered-by | 回滚 |
|----|------|------|------|------|------------|------|
| P0-01 | 双入口可见且默认负载均衡 | 进入资源接入-负载均衡 | 观察工具栏最左侧 | 出现「负载均衡 / 独占集群」radio，默认选中「负载均衡」，URL 不带 `subtype`；radio 与工具栏同一行、不额外占行（AC-001） | | — |
| P0-02 | 切到独占集群只发一次列表请求 | Network 面板已打开并清空 | 点「独占集群」 | 列表请求 `exclusive_clusters/list` **只发一次**；URL 变成 `subtype=exclusive-cluster` | | 切回「负载均衡」 |
| P0-03 | 刷新停留在独占集群 | 当前在独占集群 | 浏览器刷新 | 仍停在独占集群，筛选/分页按 URL 还原 | | — |
| P0-04 | 切换入口清掉搜索与分页 | 在独占集群里设一个筛选条件并翻到第 2 页 | 切到「负载均衡」再切回 | `filter` / `page` 被清空，两页搜索条件不串台 | | — |
| P0-05 | 列表列与空值 | 独占集群列表有数据 | 逐列核对 | 列为集群ID/集群名称/类型(TGW·STGW)/集群标签/可用区/运营商/最大连接数/集群内实例数/业务/分配状态/操作；空值统一 `--`（不是 `—`/`-`）；未分配业务列 `--` | | — |
| P0-06 | 账号树联动带上 vendor + account_id | Network 面板已打开 | 左树点到某个具体账号 | 列表请求 filter 里**同时**有 `vendor` 和 `account_id`（选中账号时 vendor 不能丢）；URL 带 `accountId` | | 切回「全部」 |
| P0-07 | 分配状态筛选生效 | 在独占集群页 | 用资源 tab 右上角切「未分配」/「已分配」 | 请求 filter 分别带 `bk_biz_id eq -1` / `bk_biz_id neq -1`；两种下的列表内容对应变化 | | 切回「全部」 |
| P0-08 | 行内单条分配 | 左树选到具体账号；存在未分配行 | 点某行「分配」 | 弹窗标题为独占集群分配并显示该行名称；目标业务下拉**非空**（来自该行 `account_id`）；选业务提交成功 Toast，列表刷新后该行业务列显示业务名、分配状态变「已分配」（AC-002） | | 该操作不可逆，用可弃用集群 |
| P0-09 | 工具栏批量分配 | 勾选 2 行未分配 | 点「批量分配」→ 选业务 → 确认 | 标题为批量分配并显示已选数量；提交后两行都变已分配（AC-002） | | 同上 |
| P0-10 | 已分配不可再分配 | 存在已分配行 | 观察该行 | 复选框禁用、不进批量集合；行内「分配」按钮禁用且 tooltip「已分配」（AC-003） | | — |
| P0-11 | 筛选条件下发正确 | Network 面板已打开 | 依次用业务、集群标签、类型、可用区、运营商、集群ID、集群名称筛选 | 每次请求 filter 与所选条件一致（字符串多 token 走 `cs`）；URL `filter` 同步；**后端上环境后**复验「仅展示命中行」（AC-004） | | 清空搜索 |
| P0-12 | 业务下拉不含未分配 | 打开筛选的「业务」下拉 | 观察选项 | 只有真实业务，没有「未分配」选项 | | — |

### P1 - 异常与边界（必测）

| ID | 场景 | 前置 | 操作 | 期望 | covered-by |
|----|------|------|------|------|------------|
| P1-01 | 列表接口失败不白屏 | 用 devtools 把 `exclusive_clusters/list` 拦成 500 | 刷新列表 | 表格仍在、显示空态，页面不白屏；可重试（AC-005） | |
| P1-02 | 分配失败提示后端文案 | 选中集合里混入 1 个已分配集群（或让接口返回失败） | 提交分配 | 弹窗提示后端 `message`，列表不做乐观更新 | |
| P1-03 | 排序走后端 | Network 面板已打开 | 点「最大连接数」「集群内实例数」表头排序 | URL 出现 `sort`/`order`，请求 `page` 带同样的 `sort`/`order`；未点排序时**不**带这两个参数 | |
| P1-04 | 其它列不可排序 | 在列表页 | 点「集群标签」「可用区」等表头 | 无排序控件、点击不改变顺序 | |
| P1-05 | 全部账号 / 只选云厂商时的分配 | 左树停在「全部」或只选到云厂商 | 打开分配弹窗 | 目标业务下拉按行自身 `account_id` 取，不因树未选账号而报错 | |
| P1-06 | 搜索条件回填展示名 | 带 `filter` 的 URL 直接打开 | 观察搜索框 | 业务等异步字段回填出**名称**而非 ID，且回填不触发额外一次列表请求 | |

### P2 - UI 细节（按需）

- [ ] 独占集群页高度与「负载均衡」页一致，工具栏+表格包在 Loading 内，无二次滚动条
- [ ] 分页器在数据量超过一页时出现，页宽 1280 / 1440 / 1920 无横向滚动
- [ ] 「分配」在无权限时禁用并有悬浮提示
- [ ] 表格空态文案正常，不是空白区域

## 验证结论

> 每条填写：PASS / FAIL / Skipped + 简要说明（FAIL 附复现步骤与截图/日志）

| ID | 结果 | 备注 |
|----|------|------|
| P0-01 | <PASS / FAIL / Skipped> | |
| P0-02 | <PASS / FAIL / Skipped> | |
| P0-03 | <PASS / FAIL / Skipped> | |
| P0-04 | <PASS / FAIL / Skipped> | |
| P0-05 | <PASS / FAIL / Skipped> | |
| P0-06 | <PASS / FAIL / Skipped> | |
| P0-07 | <PASS / FAIL / Skipped> | |
| P0-08 | <PASS / FAIL / Skipped> | |
| P0-09 | <PASS / FAIL / Skipped> | |
| P0-10 | <PASS / FAIL / Skipped> | |
| P0-11 | <PASS / FAIL / Skipped> | |
| P0-12 | <PASS / FAIL / Skipped> | |
| P1-01 | <PASS / FAIL / Skipped> | |
| P1-02 | <PASS / FAIL / Skipped> | |
| P1-03 | <PASS / FAIL / Skipped> | |
| P1-04 | <PASS / FAIL / Skipped> | |
| P1-05 | <PASS / FAIL / Skipped> | |
| P1-06 | <PASS / FAIL / Skipped> | |

- 执行日期: <YYYY-MM-DD>
- 总体结论: PASS / FAIL
- 待后端复验（不阻塞本轮）: P0-11 的命中结果、P0-08/P0-09 的真实分配落库
- 后续行动: <例如：回 coding 修 P0-xx / 提测 / 开 MR>
